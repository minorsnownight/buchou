# 产品文档规范

- `PRODUCT_SPEC.md` 只描述用户价值、页面结构、功能行为和验收结果。
- `STATE_MODEL.md` 是状态、统计口径、数据不变量和重置语义的唯一来源。
- `DESIGN_SYSTEM.md` 是视觉 Token、交互原则和通用组件的唯一来源。
- `IMPLEMENTATION_PLAN.md` 只描述实施顺序、迁移边界和每阶段验证方式。
- 文档默认简体中文；代码标识、实体名和组件名使用英文。
- 产品决策变化时先修改文档，再修改源码和测试。
- 未确认的内容必须标为“待确认”，不得写成既定行为。
