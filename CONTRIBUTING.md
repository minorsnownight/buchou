# 贡献指南

感谢你对不抽（Buchou）项目的关注！

## 开发环境

- Android Studio
- JDK 17
- Kotlin 2.3
- Android SDK 36（最低支持 API 29）

## 开发流程

1. Fork 仓库并创建分支
2. 遵循 [AGENTS.md](AGENTS.md) 中的开发规范
3. 代码改动后运行测试：`./gradlew testDebugUnitTest`
4. 构建验证：`./gradlew assembleDebug`
5. 提交 PR，描述改动内容和关联问题

## 代码规范

- 遵循现有代码风格
- 不添加需求之外的功能、抽象或灵活性
- 密钥、密码不进代码、不进 commit、不进日志
- 改完主动跑验证，不注释报错或加绕过标记
- 只修改当前需求直接涉及的文件

## 设计原则

- 状态先于操作：先告诉用户现在是什么状态，再提供下一步
- 一致性先于装饰：同一层级只能有一套字号、间距和圆角
- 简单不是隐藏：重要状态和后果必须明确表达
- 深色模式不是反色：重新校准表面层级、文字对比度和状态色

## 文档优先

产品决策变化时先修改 `docs/` 下的文档，再修改源码和测试。
