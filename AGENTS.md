# buchou 开发规范

## 产品范围

- Android 优先，应用 ID 为 `com.buchou.app`，最低支持 Android 10（API 29）。
- Android 使用 Kotlin、Jetpack Compose 和原生系统能力；iOS 后续使用 Swift/SwiftUI。
- 数据本地优先，不引入业务后台；跨设备同步仅使用用户配置的 WebDAV。
- 默认简体中文，同时支持英文；界面必须兼容深色模式。

## 目录与命名

- Android 工程位于 `android/`，用户可安装产物放入 `outputs/`。
- 临时文件、调研脚本和中间产物放入 `work/`，不得提交到应用源码目录。
- Kotlin 包统一位于 `com.buchou.app` 下，按职责使用 `data`、`domain`、`ui`、`alarm`、`widget`、`sync`。
- 时间戳持久化为 UTC Instant；自然日记录必须同时保存产生时的时区。

## 实现原则

- 只实现当前阶段需求，不提前搭建服务器、账号、会员或跨端共享框架。
- 连续不抽天数、日期状态、统计和同步合并规则必须集中在 domain 层并有单元测试。
- 漏打卡不打断连续不抽天数；只有吸烟事件会重置当前连续时长。
- 页面统一使用设计 Token、应用脚手架和同一个底部 Tab Bar，不允许页面自行复制导航样式。
- 闹钟默认关闭；用户启用时才请求精确闹钟权限。
- 打卡后只取消当天闹钟，并安排下一个启用日。
- WebDAV 同步失败不得覆盖本地数据，凭据不得进入数据库、日志或同步文件。
- 健康里程碑必须附权威来源并明确不是医疗建议。

## 验证

- 每次 Kotlin 逻辑改动至少运行相关单元测试。
- 每个阶段结束运行 `./gradlew testDebugUnitTest assembleDebug`。
- 闹钟、重启恢复、桌面小组件和本地音频必须在 真机验收。
- 不通过注释错误、关闭检查或添加忽略标记来绕过失败。

## 安全与变更边界

- 不写入密钥、WebDAV 密码、真实账号或设备隐私数据。
- 删除文件、修改 CI/CD、发布应用、推送 Git 历史前必须获得用户确认。
- 仅修改当前需求直接涉及的文件；发现无关问题只记录，不顺手重构。
